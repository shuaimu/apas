# Task 8.5 evidence — preserve-terminal-agents-across-cli-reboots

Host: zoo-005 · Server: apas.mpaxos.com · Recorded: 2026-08-15T02:58Z
Deployed: apas-server + web 26.08.48 (server → web → CLI, 2026-08-15T02:48–02:52Z)

## Clause 1 — deploy server/web compatibility support before upgraded CLIs
PASS. Deployed in server → web → CLI order. Server binary md5-verified against
the local build before the web was synced. Confirmed the deployed server carries
the lenient lifecycle decode ("ignoring a lifecycle request for a retired
operation" present; "reconnect_transport" absent).

## Clause 2 — verify health
PASS. apas-server / apas-web / nginx active. Public pages / /login /machines
/share /admin /health all 200 with 0 redirects; referenced _next static chunk
200; cleartext :80 health 200 (legacy CLI path). No error-priority journal
entries for either unit. apas-server RSS 39.8 MB; container 159 MB / 16384 MB.

## Clause 3 — verify mixed-version warnings
PASS (protocol level). A pre-capability CLI (26.08.31, predates cli_lifecycle_v1
introduced in 008103d) was attached to the production server from a scratch
project.

  - It registered successfully over wss and REMAINED connected for the
    observation period; the new server logged no errors. Mixed-version
    coexistence holds — the new server does not disconnect old CLIs.
  - The old binary contains none of the lifecycle message types:
        cli_lifecycle_v1 = 0, cli_lifecycle_inventory = 0,
        cli_lifecycle_status = 0, cli_lifecycle_request = 0
    (the shipped 26.08.48 binary contains all four)
  - Consequently the server records no CLI_LIFECYCLE_CAPABILITY for that
    session, so lifecycle_cli_message() returns None and the client receives a
    Failed status reading "This project CLI is too old for safe lifecycle
    controls. Upgrade it on the project host." — never a substituted reboot.
    Covered by unit test an_old_cli_receives_no_lifecycle_request_at_all.
  - No inventory is published for that session, so the web's rebootCli takes
    the "too old" toast branch rather than the correlated path.

NOT COVERED: a browser screenshot of the rendered warning. The chain above is
protocol-level observation plus unit tests; the visual confirmation was not
performed.

## Clause 4 — confirm no abandoned pane hosts after the observation window
PASS. 7 pane-host runtimes on zoo-005, every one with a live tmux session AND an
ESTABLISHED controller connection on its c.sock (verified with `ss -xp`; an
earlier check using /proc/*/fd inode comparison was wrong — socket-table inodes
are not filesystem inodes — and its "no controller" result is retracted).

  project      pane  tmux   controller pid
  4366dc38     189   ALIVE  3876098   (~/mako)
  73757577     305   ALIVE  1964252   (~/mako-cloud)
  73757577     826   ALIVE  1209561   (~/mako-cloud)
  73757577     888   ALIVE  3882775   (~/mako-cloud)
  f150d041     7     ALIVE  3071328   (~/apas)
  f150d041     294   ALIVE  1912964   (~/apas)
  f150d041     423   ALIVE  3887567   (~/apas)

Oldest runtimes are ~22 h old. An orphan self-terminates when its lease expires
(default 600 s), so surviving 22 h is itself evidence each retained a controller.
No tombstones present (no cleanup mid-flight).

Three tmux sockets under /tmp/tmux-3000 have NO sessions
(9ea02fb0, ca0a71cf, e58bdefb) — leftover socket files after their last session
ended, not abandoned hosts. No tmux server, no provider process. Cosmetic.

## Caveat that should be recorded with this pass
All 7 controllers are DISCONNECTED from the server. The pane hosts and their
provider processes are alive and locally controlled, but their projects are
invisible in the web. This is the designed behaviour (controller loss must not
kill the host), but it means "no abandoned hosts" is passing while 7 live agents
are unreachable from the UI. Tracked separately from 8.5.
